import Phaser from "phaser";
export default class Vehicle extends Phaser.GameObjects.Container { constructor(scene,path,color=0xef4444){ super(scene,path[0].x,path[0].y); const g=scene.add.graphics(); g.fillStyle(0x000,.2); g.fillEllipse(0,8,34,10); g.fillStyle(color,1); g.fillRoundedRect(-18,-7,36,16,5); g.fillStyle(0xdbeafe,1); g.fillRect(-8,-11,16,7); g.fillStyle(0x111827,1); g.fillCircle(-10,9,4); g.fillCircle(11,9,4); this.add(g); scene.add.existing(this); this.setDepth(2000); const tl=scene.tweens.createTimeline(); for(const p of path.slice(1)) tl.add({targets:this,x:p.x,y:p.y,duration:Phaser.Math.Distance.Between(this.x,this.y,p.x,p.y)*18,ease:"Linear",onUpdate:()=>this.setDepth(this.y+200)}); tl.setLoop(-1); tl.play(); }}
